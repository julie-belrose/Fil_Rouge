{
  "name": "fil-rouge",
  "version": "1.0.0",
  "type": "module",
  "description": "Gestion des déchets - HTML/CSS/JS avec Express.js",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "commit": "cz",
    "start": "node backend/app.js",
    "dev": "nodemon backend/app.js",
    "build:frontend": "rm -rf frontend/dist && mkdir frontend/dist && cp -r frontend/public/* frontend/dist/ && cp -r frontend/src/* frontend/dist/",
    "check:env": "node backend/src/utils/validateEnv.js",
    "init:mongo": "node scripts/initMongo.js",
    "seed:mongo": "node scripts/seedMongo.js",
    "init:mysql": "node scripts/initMySQL.js",
    "seed:mysql": "node scripts/seedMySQL.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.0",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "commitizen": "^4.3.1",
    "cz-conventional-changelog": "^3.3.0"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  }
}
